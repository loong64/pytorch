name: custom build

on:
  workflow_dispatch:
    inputs:
      app_version:
        description: 'Build version'
        required: true
        default: ''

jobs:
  custom:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v6
        with:
          repository: pytorch/pytorch
          ref: ${{ inputs.app_version }}
          submodules: recursive
          fetch-depth: 2

      - name: Prepare build
        run: |
          app_version=${{ inputs.app_version }}
          wget -qO - https://github.com/loong64/pytorch/raw/refs/heads/main/patch_loong64.patch | patch -p1

          wget -qO - https://github.com/loong64/pytorch/raw/refs/heads/main/patch_loong64.patch | patch -p1

          cd third_party/sleef
          wget -qO - https://github.com/loong64/pytorch/raw/refs/heads/main/sleef/sleef_loong64.patch | patch -p1
          cd -

          cd third_party/cpuinfo
          wget -qO - https://github.com/loong64/pytorch/raw/refs/heads/main/cpuinfo/cpuinfo_loong64.patch | patch -p1
          cd -

          echo "PYTORCH_BUILD_NUMBER=1" >> $GITHUB_ENV
          echo "PYTORCH_BUILD_VERSION=${app_version/v/}" >> $GITHUB_ENV
          echo "PIP_EXTRA_INDEX_URL=https://gitlab.com/api/v4/projects/65746188/packages/pypi/simple" >> $GITHUB_ENV

          mkdir -p wheelhouse
          chmod +x .ci/manywheel/build.sh

      - name: Setup QEMU
        uses: docker/setup-qemu-action@v4

      - name: Build wheels
        env: 
          BUILD_ENVIRONMENT: linux-loongarch64-binary-manywheel
          PYTORCH_FINAL_PACKAGE_DIR: /artifacts
          PYTORCH_ROOT: /pytorch
          PACKAGE_TYPE: manywheel
          DESIRED_CUDA: cpu
          GPU_ARCH_TYPE: cpu-loongarch64
          DOCKER_IMAGE: ghcr.io/loong64/manylinuxloongarch64-builder:cpu-loongarch64
          DESIRED_PYTHONS: "3.10 3.11 3.12 3.13 3.14 3.14t"
          BUILD_NAME_PREFIX: manywheel-py
          BUILD_NAME_SUFFIX: -cpu-loongarch64
        run: |
          DOCKER_CMD="${PYTORCH_ROOT}/.ci/manywheel/build_all.sh"
          docker run --rm \
            --platform linux/loong64 \
            --env PYTORCH_ROOT \
            --env PACKAGE_TYPE \
            --env DESIRED_CUDA \
            --env GPU_ARCH_TYPE \
            --env SKIP_ALL_TESTS=1 \
            --env DESIRED_PYTHONS \
            --env BUILD_ENVIRONMENT \
            --env PYTORCH_BUILD_NUMBER \
            --env PYTORCH_BUILD_VERSION \
            --env PYTORCH_FINAL_PACKAGE_DIR \
            --env BUILD_NAME_PREFIX \
            --env BUILD_NAME_SUFFIX \
            --env PIP_EXTRA_INDEX_URL \
            --volume $(pwd):${PYTORCH_ROOT} \
            --volume $(pwd)/wheelhouse:/${PYTORCH_FINAL_PACKAGE_DIR} \
          ${DOCKER_IMAGE} sh -c "${DOCKER_CMD}"

      - name: Upload wheels
        run: |
          pip install twine==6.0.1
          for file in wheelhouse/*.whl; do
            twine upload --repository-url https://gitlab.com/api/v4/projects/65746188/packages/pypi $file || true
          done
        env:
          TWINE_USERNAME: ${{ github.repository_owner }}
          TWINE_PASSWORD: ${{ secrets.GL_TOKEN }}

      - name: Upload release
        run:
          gh release upload ${{ needs.check.outputs.version }} -R ${{ github.repository }} wheelhouse/*.whl --clobber
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}